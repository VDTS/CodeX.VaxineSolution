<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             x:Class="VaxineApp.Views.Home.Family.EditFamilyPage"
             Shell.TabBarIsVisible="False"
             Shell.PresentationMode="Modal"
             Title="Edit Family">
  <ContentPage.Content>
    <StackLayout Padding="40" Spacing="10">
      <Entry Text="{Binding Family.HouseNo}"
                                 Placeholder="House No."
                                 Keyboard="Numeric"></Entry>
      <Entry Text="{Binding Family.ParentName}"
                                 Placeholder="Parent Name"></Entry>
      <Entry Text="{Binding Family.PhoneNumber}"
                                 Placeholder="Parent Number"></Entry>
      <Button Text="Save"
              TranslationY="50"
              WidthRequest="130"
              HeightRequest="50"
              HorizontalOptions="Center"
              Command="{Binding PutCommand}"></Button>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>