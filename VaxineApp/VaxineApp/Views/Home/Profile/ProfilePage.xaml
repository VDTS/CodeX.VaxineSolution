<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VaxineApp.Views.Home.Profile.ProfilePage"
             xmlns:local="clr-namespace:VaxineApp.Renderers"
             xmlns:ViewModel="clr-namespace:VaxineApp.ViewModels.Home.Profile"
             Title="Profile">
  <ContentPage.BindingContext>
    <ViewModel:ProfileViewModel></ViewModel:ProfileViewModel>
  </ContentPage.BindingContext>
  <ContentPage.ToolbarItems>
    <ToolbarItem Command="{Binding GoToPutPageCommand}"
                 Priority="0"
                 Text="Edit"
                 Order="Secondary"
                 />
  </ContentPage.ToolbarItems>
  <ContentPage.Content>
    <ScrollView>
    <StackLayout Padding="40" Spacing="10">
        <Frame CornerRadius="100"
               HeightRequest="180"
               WidthRequest="180"
               HorizontalOptions="Center"
               Padding="0"
               IsClippedToBounds="True">
          <Image Source="ProfilePhoto.jpg"
                 HorizontalOptions="Center"
                 VerticalOptions="Center">
          </Image>
        </Frame>
        <BoxView   VerticalOptions="Center"
                 HorizontalOptions="Center"
                 HeightRequest="30"
                 WidthRequest="200"></BoxView>
      <Label Text="Name"></Label>
      <Label FontSize="Large" Text="{Binding Profile.FullName}"></Label>
      <Label Text="Gender"></Label>
      <Label FontSize="Large" Text="{Binding Profile.Gender}"></Label>
      <Label Text="Father or Husband Name"></Label>
      <Label FontSize="Large" Text="{Binding Profile.FatherOrHusbandName}"></Label>
      <Label Text="Age"></Label>
      <Label FontSize="Large"
               Text="{Binding Profile.Age}"></Label>
      <Label Text="Role"></Label>
        <Label FontSize="Large"
               Text="{Binding Profile.Role}"></Label>
      </StackLayout>
    </ScrollView>
  </ContentPage.Content>
</ContentPage>